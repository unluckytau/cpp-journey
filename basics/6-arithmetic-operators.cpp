#include <iostream>

int main(){
  double students = 20;

  // additions 
    students = students + 1;
    students+=1;
    students++; // increment operator

  // subtractions
    students = students - 1;
    students -=1;
    students--; //decrement operator

  // multiplication
    student = students * 2;
    students*=2;

  // division
    students = students / 2;
    students/=2;
    students/=3; // requires double data type

  // find remainders
    int remainder = students % 3;

  // B.O.D.M.A.S
  // to force priority to an operator, wrap them in paranthesis ()
    int students = 6 - 5 + 4 * 3 / 2;

  return 0;
}
